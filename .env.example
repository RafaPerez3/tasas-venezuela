# Copia este archivo como ".env" (ese sí está ignorado por git) y pon tus datos reales.
# En Render: Settings -> Environment -> Add Environment Variable (las dos de abajo).

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
